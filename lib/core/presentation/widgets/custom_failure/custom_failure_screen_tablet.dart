import 'package:flutter/material.dart';

/// this screen is used to show the deposit failure screen for tablet
class CustomFailureScreenTablet extends StatelessWidget {
  /// constructor
  const CustomFailureScreenTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
